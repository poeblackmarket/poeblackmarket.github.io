#
# Sockets/Links
#
(\d{1})s:
 query: sockets.socketCount:>=$1
(\d{1})l:
 query: sockets.largestLinkGroup:>=$1
(\d{1})s(\d{1})l:
 query: sockets.socketCount:>=$1 sockets.largestLinkGroup:>=$2
(\d{1})l(\d{1})s:
 query: sockets.socketCount:>=$2 sockets.largestLinkGroup:>=$1